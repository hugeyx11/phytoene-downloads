# Phytoene Downloads

Static download centre for `download.phytoene.cn`. Publish signed installation files through GitHub Releases, then replace the Cloning Primer Designer link in `index.html` with the release asset URL.
